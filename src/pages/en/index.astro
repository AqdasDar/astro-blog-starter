---
// src/pages/en/index.astro
import {getCollection} from 'astro:content';
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import ProductionGallery from '../../components/ProductionGallery.astro';
import {SITE_TITLE, SITE_DESCRIPTION} from '../../consts';

const productions = (await getCollection('productions'))
    .filter((p) => p.id.startsWith('en/'))
    .sort(
        (a, b) => b.data.releaseDate.valueOf() - a.data.releaseDate.valueOf()
    );
---
<html lang="en">
<head>
    <BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION}/>
    <style>
        main {
            max-width: 960px;
        }

        .intro {
            padding-bottom: 2rem;
            border-bottom: 1px solid #ddd;
            margin-bottom: 2rem;
        }

        html.dark .intro {
            border-bottom-color: rgb(var(--gray-light));
        }
    </style>
    <title></title>
</head>
<body>
<Header/>
<main>
    <section class="intro">
        <h1>Welcome to my portfolio</h1>
        <p>
            I am a director passionate about creating unique productions.
            Discover my latest work below.
        </p>
    </section>

    <section>
        <ProductionGallery productions={productions}/>
    </section>
</main>
<Footer/>
</body>
</html>
