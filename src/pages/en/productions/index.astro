---
import {getCollection} from 'astro:content';
import BaseHead from '../../../components/BaseHead.astro';
import Header from '../../../components/Header.astro';
import Footer from '../../../components/Footer.astro';
import ProductionGallery from '../../../components/ProductionGallery.astro';
import FilterControls from '../../../components/FilterControls.astro';
import {SITE_TITLE} from '../../../consts';

const productions = (await getCollection('productions'))
    .filter((p) => p.id.startsWith('en/'))
    .sort(
        (a, b) => b.data.releaseDate.valueOf() - a.data.releaseDate.valueOf()
    );
---

<!doctype html>
<html lang="en">
<head>
    <BaseHead title={`Our Video and Cinematic Productions - Aesthetic Production`} description="Discover the complete lineup of audiovisual productions by Aesthetic Production. Explore our portfolio featuring our directed short films, captivating documentaries, and high-quality corporate videos."/>
    <style>
        main {
            max-width: 960px;
        }
    </style>
</head>
<body>
<Header/>
<main>
    <h1>Our Productions</h1>
    <FilterControls />
    <section>
        <ProductionGallery productions={productions}/>
    </section>
</main>
<Footer/>
</body>
</html>
